#include "doublearray.h"

namespace math {
    string_ptr DoubleArray::toString(double_array m, int n) {
	using namespace std;
	string_ptr str(new string());
	for (int x = 0; x != n; x++) {
	    //str->insert(m[x]);
	};
	return str;
    };
};

