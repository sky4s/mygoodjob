#ifndef CPH
#define CPH

//C系統文件

//C++系統文件

//其他庫頭文件

//本項目內頭文件


namespace cms {
    namespace measure {
	namespace cp {
	    /*class MeasureInterface {
	      public:
		virtual bptr < MeasureResult >
		    measureResult(RGB_vector_ptr rgbVec, bool forceTrigger,
				  bool trigger) = 0;

		virtual Patch_ptr measure(RGB_ptr rgb, bool forceTrigger,
					  bool trigger) = 0;
		virtual Patch_ptr measure(RGB_ptr rgb) = 0;
		virtual void reset() = 0;
		//int[] getMeasureCount();
		//void triggerMeasure(MeasureTrigger measureTrigger);
	    };*/
	};
    };
};
#endif

